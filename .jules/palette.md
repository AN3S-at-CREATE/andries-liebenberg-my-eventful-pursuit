## 2026-02-17 - Accessibility: Social Links
**Learning:** Icon-only social links (GitHub/LinkedIn) are invisible to screen readers without `aria-label`. This pattern is common in navbars and footers.
**Action:** Always add `aria-label="[Platform Name] Profile"` to icon-only links to ensure they are accessible.
